package com.amirhn.Moves;

/** The enum Move type. */
public enum MoveType {
  /** Walk move type. */
  WALK,
  /** Capture move type. */
  CAPTURE,
  /** Castling move type. */
  CASTLING,
  /** Promotion move type. */
  PROMOTION
}
